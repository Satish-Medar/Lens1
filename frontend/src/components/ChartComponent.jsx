import React from 'react';

const ChartComponent = () => {
  return (
    <div style={{ height: '100%', border: '2px dashed #ccc', display: 'flex', alignItems: 'center', justifyContent: 'center', borderRadius: '8px' }}>
      <h2 style={{ color: '#aaa' }}>Chart Area</h2>
    </div>
  );
};

export default ChartComponent;